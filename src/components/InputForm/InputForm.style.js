import {makeStyles} from "@material-ui/core";

const styles = makeStyles(theme => ({
    textField: {
        minWidth: "100%",
        maxWidth: "100%",
        flexGrow: 1
    },
    button: {
        minWidth: "100%",
        maxWidth: "100%",
        flexGrow: 1
    },
    root: {
        flexGrow: 1
    }
}));

export default styles;
